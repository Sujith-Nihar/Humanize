# P1-S13-T02 — Complete independent verifier

Status: in_progress

## Objective and prerequisites

Complete independent verifier. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S13-T01. Requirements: FR-13, FR-14, FR-15, NFR-05.
Upstream tasks: P1-S8-005.

## Implementation scope

Expected modules: `review`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Corrected output is revalidated and cannot fabricate evidence.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T18:18:19.589115+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Verifier corrections are now revalidated rather than trusted. DEFECT FOUND AND FIXED: correctedExplanation and correctedReplacement were applied verbatim, so a verifier could replace a reviewer suggestion with text that drops a placeholder, producing a message with a hole in it, or blank out an explanation. Corrections are model output like any other and are now checked before use: a corrected replacement must preserve the exact placeholder multiset of the reviewed text, a corrected explanation must be non-empty after trimming, and a correction failing either check is discarded so the reviewer original stands. A discarded correction is recorded as a CORRECTION_DROPPED_PLACEHOLDER diagnostic rather than passing silently, and ReviewOutcome now carries diagnostics. Verified by test: a verdict proposing "Hi there, you have alerts" against text containing {name} and {{count}} is refused and the reviewer replacement survives, while a sound correction preserving both placeholders is applied. Results: pnpm check 191 unit tests across 31 files; pnpm build passed; 40 integration tests passed.
- Next action / remaining acceptance: A corrected replacement is not yet re-run through the parser-safe suggestion validator, so it is placeholder-checked but not structurally proven; wiring buildSuggestion over corrected output is the remaining step. A verifier cannot fabricate evidence, since evidence references are fixed at candidate validation and a verdict carries none.
