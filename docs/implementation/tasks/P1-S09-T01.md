# P1-S09-T01 — Extract HTML text and attributes

Status: in_progress

## Objective and prerequisites

Extract HTML text and attributes. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S08. Requirements: FR-06, FR-07, NFR-03, NFR-04.
Upstream tasks: P1-S4-003.

## Implementation scope

Expected modules: `extractors`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Entities/quotes mapped; scripts/styles/machine attributes excluded.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T16:55:04.719363+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: HTML extraction implemented with parse5 and source-location tracking, closing the gap the corpus measured. Rendered text nodes and visible or accessibility attributes (alt, title, placeholder, aria-*, label, summary) are extracted; script, style, template, noscript, svg, code, pre, iframe and object contents are excluded, as are machine attributes such as href, src, class, id, name, type and value however wordy their values look. Source ranges map exactly onto the original document including multi-line text, quote style is recorded so an attribute value can be re-encoded safely, and entity-bearing content is marked suggestionSafe false with encoding entity because the raw source and rendered text differ and a verbatim replacement would corrupt the encoding. Element name drives content kind, so a heading, link and alt text are classified distinctly. 7 unit tests cover visible-only extraction, machine attribute exclusion, exact single and multi-line spans, entity handling, quote style, kind classification, and recovery from malformed markup without failing the file. MEASURED EFFECT on the fixture corpus, same model and fixtures as before: extracted nodes rose from 8 to 13 and reviewed changed nodes from 4 to 7, and index.html now produces findings, including clarity on "The ultimate game-changing solution for modern enterprises" which was previously invisible. Results: pnpm check 153 unit tests across 27 files; pnpm build passed.
- Next action / remaining acceptance: Acceptance for entities, quotes and exclusions is met at unit level. Remaining: HTML-specific suggestion encoding is not implemented, so an entity-bearing or attribute finding currently falls back to comment-only; that belongs with P1-S11. Extraction precision and recall against a labelled HTML corpus are not measured, which is P1-S09-T04 and P1-S10-T04.
