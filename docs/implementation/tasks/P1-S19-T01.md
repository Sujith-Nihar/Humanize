# P1-S19-T01 — Complete telemetry and alerts

Status: in_progress

## Objective and prerequisites

Complete telemetry and alerts. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S18. Requirements: SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, NFR-03, NFR-04, NFR-05, NFR-06.
Upstream tasks: P1-S11-001, P1-S11-002.

## Implementation scope

Expected modules: `telemetry`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

End-to-end traces and leakage tests cover all boundaries.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T22:33:17.968487+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Telemetry completed as far as it can be without live services. The operational measurements the specification lists are now a typed union recorded through one surface, so a stage cannot invent its own label and a dashboard does not depend on spelling: webhook ingest, queue wait, checkout, extraction, context retrieval, model call, publication, retries and failures, stale reviews, runner state, findings published and suppressed, suggestions offered and accepted, verifier suppression, duplicates merged, files classified and nodes extracted. INSTRUMENTATION CANNOT BECOME A LEAK, which is the property worth having: a measurement dimension is emitted output like any other, so it passes the same field allowlist and the same shape-based scrubbing as a log line. Tests prove a bearer token and a provider key in dimensions are redacted, an unexpected dimension name is dropped entirely, and that repository content passed as text, explanation, quote or replacement never reaches output in any form; even a file path is refused, because a path can identify private work. Earlier in this stage the same review found that redaction filtered by field name only and let a connection string through as event; that is fixed and covered. Results: pnpm check 217 unit tests across 36 files; pnpm build passed.
- Next action / remaining acceptance: Not satisfied: the task requires end-to-end traces across all boundaries, and no stage yet calls measure or span in production paths, so the surface exists and is safe but nothing is actually instrumented; wiring it belongs with the stages it measures. No OpenTelemetry exporter is configured, so nothing is scraped or collected anywhere, and no alerting rules exist. A real trace across webhook, queue, runner and publication cannot be observed until the GitHub bindings exist.
