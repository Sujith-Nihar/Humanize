# P1-S08-T06 — Connect local runner execution

Status: in_progress

## Objective and prerequisites

Connect local runner execution. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S08-T05. Requirements: FR-11, FR-12, FR-13, FR-14, FR-20, FR-21, NFR-05.
Upstream tasks: P1-S7-005.

## Implementation scope

Expected modules: `apps/runner`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Private live repository review has no cloud-model traffic or retained source.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-20T23:39:40.029007+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: A PRIVATE LIVE REPOSITORY WAS REVIEWED END TO END, which this task names. Two runs against a real private pull request: with qwen3:8b, 74 files inspected, 123 ContentNodes extracted, 12 changed nodes reviewed in 788 seconds; with llama3.2, the same work in 572 seconds, followed by a published check run and comment. Lease renewal held across a 13 minute job, so the protocol survives a long review. The workspace was destroyed on every path and stayed at 456 KiB, confirming the blobless acquisition fix under production conditions. No cloud model was contacted: execution is local Ollama and the snapshot refuses a non-Ollama profile for a runner job. BOTH RUNS PRODUCED ZERO PUBLISHED FINDINGS, and the diagnostics say why rather than leaving it silent: SUPPRESSED_VERIFIER_SUPPRESSED 9, SUPPRESSED_TEXT_NOT_IN_NODE 2, SUPPRESSED_BELOW_CONFIDENCE 2, REVIEW_FAILED_TIMEOUT 1, FILE_UNREADABLE 1. The reviewer proposed roughly 13 candidates and every one was discarded. Notably the deterministic gate caught TWO fabricated quotations in production, which is the fabrication defence working on real model output.
- Next action / remaining acceptance: QUALITY GAP, MEASURED NOT ASSUMED: the deterministic rules produced ZERO signals across all 12 changed nodes, although the copy is textbook AI-styled writing. Its shape is em-dash contrast constructions and negation reframing ("It is Not Just Music — It is", "intentionally designed — not randomly generated", "no guesswork, just sound engineered", "In a world of endless playlists"). The rule families are phrase-list and density based and have no rule for that shape at all, so nothing fires. Adding one must go through the gold corpus first, because precision matters more than recall here. Separately the verifier suppressed 9 of 13 candidates on a 3B model, which needs measuring against the labelled corpus before concluding whether the verifier or the reviewer is at fault.
