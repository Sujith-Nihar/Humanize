# P1-S11-T04 — Render diff-eligible GitHub suggestions

Status: in_progress

## Objective and prerequisites

Render diff-eligible GitHub suggestions. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S11-T03. Requirements: FR-16, SEC-07.
Upstream tasks: P1-S9-004, P1-S9-005.

## Implementation scope

Expected modules: `suggestions`, `github`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Live apply succeeds; unrepresentable ranges become comment-only.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-22T00:47:54.937800+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: One-click suggestions are published and applicable on a real private pull request, and the control plane derives every patch itself through a transient GitHub read rather than trusting the runner (ADR-026, INV-005). Measured live: 3 of 5 inline comments carried a suggestion. A tagline fix rewrote text inside a JSX span leaving the element and className untouched; a model rewrite preserved a trailing JSX expression on the same line. TWO DESTRUCTIVE SUGGESTIONS REACHED A PULL REQUEST BEFORE BEING CAUGHT, both from the model: replacing the button text "Get Started - Listen Now" with the single character "-", and truncating a sentence to its trailing clause. A facts-only information check passed both because neither contains a number or a brand name. A replacement must now retain at least 0.7 of the original content words, a floor derived from the case the user rejected rather than guessed: deleting "- Designed for the Mind" from a heading leaves 0.6. Content-word retention was tried as a second check and removed, because recasting "cuts review time" as "reviews faster" drops three content words while saying the same thing. Rules now propose a deletion only where what is removed is a negation of the claim beside it. Comments also carry a collapsed prompt for coding agents that states the constraint, not just the complaint.
- Next action / remaining acceptance: No human has applied a suggestion, which is the live-apply acceptance this task names. The rewrite loop is planned but not built: propose, run the deterministic gates, revise ONCE on concrete failure, then one verifier pass for meaning (stage 4 of docs/implementation/plan-production-review.md, needs an ADR). Length catches deletion; nothing cheap catches a changed meaning, which is the acknowledged limit of the gate.
