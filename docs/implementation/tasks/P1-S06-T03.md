# P1-S06-T03 — Deduplicate and recover deliveries

Status: in_progress

## Objective and prerequisites

Deduplicate and recover deliveries. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S06-T02. Requirements: FR-01, FR-02, FR-17, SEC-04, SEC-06, NFR-02.
Upstream tasks: P1-S2-003.

## Implementation scope

Expected modules: `apps/api`, `queue`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Atomic enqueue and replay/recovery produce one intended run.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-20T21:31:46.176644+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Delivery ingestion and deduplication proven END TO END against live GitHub. A real installation.created delivery was redelivered through GitHubTokenBroker.redeliver, travelled GitHub to smee to the Fastify endpoint, passed raw-body HMAC verification, normalized, and committed as one transaction: webhook_deliveries row plus a github.event job in pg-boss. INV-009 proven live: redelivering the SAME delivery returned 202 again and left exactly one delivery row and one job. The worker then consumed the job and recorded github_installations 163328826 with suspended=false and deleted=false, and the job reached state completed. Chain verified: GitHub to smee to HMAC to normalize to Postgres to pg-boss to worker to durable state.
- Next action / remaining acceptance: Scheduled failed-delivery recovery is still absent: redelivery was driven manually here, and nothing periodically reconciles missed deliveries using the App deliveries API. Debounce of synchronize events and lifecycle cancellation also remain unimplemented.
