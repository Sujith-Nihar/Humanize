# P1-S07-T01 — Create runner lease persistence

Status: complete

## Objective and prerequisites

Create runner lease persistence. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S06. Requirements: FR-20, SEC-04, SEC-05, NFR-01.
Upstream tasks: P1-S1-003.

## Implementation scope

Expected modules: `db`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Concurrent claims produce one fenced lease.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T12:59:16.900865+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Acceptance criterion proven against PostgreSQL 18.6: concurrent claims produce one fenced lease. Migrations 0000-0003 apply from a freshly created disposable database and re-apply idempotently. runner_leases carries fence, attempts, expiry and result_hash with composite tenant foreign keys and UNIQUE(run_id); two simultaneous claims grant exactly one lease under FOR UPDATE SKIP LOCKED, with the persisted row showing LEASED, attempts=1 and the fence returned to the winner. Expiry reclamation increments the fence and invalidates the previous one on renewal; revocation cancels live leases. SUBSEQUENT FIX (2026-09-18): lease liveness is now decided by the database clock. RunnerStore.accept previously compared a PostgreSQL timestamp against the API process clock, so any skew between the two machines could accept a lease the database had already expired and possibly reassigned, which risks two runners submitting results for one review; the comparison moved into SQL and claim/renew now also return expiresInMs so a remote runner never depends on clock agreement either. Covered by a regression test that forces the process clock an hour behind and an hour ahead and asserts the database decision holds both ways. 40 integration tests and 99 unit tests pass; pnpm check and pnpm build pass.
- Next action / remaining acceptance: No remaining acceptance for lease persistence. Revisit only if the lease protocol changes; note that expiresInMs is now part of the claim and renewal contract.
