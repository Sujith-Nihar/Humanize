# P1-S19-T03 — Run resource and outage stress tests

Status: in_progress

## Objective and prerequisites

Run resource and outage stress tests. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S19-T02. Requirements: SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, NFR-03, NFR-04, NFR-05, NFR-06.
Upstream tasks: P1-S11-008.

## Implementation scope

Expected modules: `testing`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Limits respected, coverage honest and service responsive.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T22:31:56.425088+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Resource and outage stress implemented in evals/security/limits.test.ts, driving the real components past each documented limit. LIMITS ARE ENFORCED, NOT JUST DOCUMENTED: a file past LIMITS.fileBytes yields no nodes and a FILE_TOO_LARGE diagnostic; a file past LIMITS.nodesPerFile stops at the cap and reports NODE_LIMIT_REACHED; a single node longer than LIMITS.nodeChars is refused rather than truncated, because a truncated node would be reviewed as if it were the whole sentence; context stays within LIMITS.contextTokens with a crowded index and within a tighter caller budget, reporting truncation so nobody mistakes partial context for the repository. RESPONSIVENESS: indexing 5000 nodes and searching them completes well under five seconds. OUTAGE: a provider failing on one node records a failure and still reviews the rest, with reviewed counting both. COVERAGE IS HONEST: a non-locale data file, an unsupported format and an oversized file each produce zero nodes AND a named diagnostic, so a skip is never silent; this is the failure mode the project has rediscovered repeatedly and the test asserts against it directly. Results: pnpm check 215 unit tests across 36 files; pnpm build passed; 70 integration tests passed.
- Next action / remaining acceptance: Not covered: no test drives a full repository through acquisition, extraction and review at scale, because that needs a large real repository and GitHub credentials; parser wall-clock timeouts (LIMITS.parserMs) and memory bounds are defined but not enforced by an isolated worker, so a pathological parser can still block its job; there is no load test of the API or queue under concurrent webhook volume, and no global rate limit on unauthenticated endpoints.
