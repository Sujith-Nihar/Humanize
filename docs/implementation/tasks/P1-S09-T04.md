# P1-S09-T04 — Extend placeholder and failure corpus

Status: in_progress

## Objective and prerequisites

Extend placeholder and failure corpus. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S09-T03. Requirements: FR-06, FR-07, NFR-03, NFR-04.
Upstream tasks: P1-S4-010, P1-S4-011.

## Implementation scope

Expected modules: `evals/extraction`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Malformed files isolated; all metrics reported.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T17:20:57.581092+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Extraction corpus expanded from 7 to 17 labelled files covering every parser, now carrying placeholder expectations, suggestion-eligibility expectations and hostile inputs: deep nesting, unclosed HTML, entity-bearing text and attributes, escaped locale values, ICU and printf placeholders, digest-like values, table markup, inline code inside prose, and dynamic Vue and Svelte bindings. The gate enforces four properties in the default lane and now fails pnpm check on a regression in any of them. THE EXPANDED CORPUS IMMEDIATELY FOUND FOUR DEFECTS, all fixed. First, Markdown dropped inline code from inside a paragraph, so "Each installation may send `5000` requests per hour." was reviewed as "Each installation may send  requests per hour." with the value missing; a finding or a suggestion based on that corrupted sentence would have been wrong, so inline code is now included as composite-encoded text and the node is not offered as a replacement. Second, Markdown emitted raw table markup as a paragraph, because remark without the GFM plugin does not parse tables; pipe-delimited blocks are now skipped, and full table-cell extraction still requires remark-gfm and is recorded as a gap. Third, locale extraction reviewed digest-like values as prose, so a commit SHA under a build.sha key became reviewable content; sha, hash, digest, token, secret and slug keys are now machine keys, and an unspaced hex or identifier token is refused by shape. Fourth, the range-accuracy metric itself was wrong: it demanded a byte-identical slice for every segment, which an entity-encoded or escaped segment can never satisfy; identity segments must now slice exactly, and any other encoding must have a real span and a node not offered as a verbatim replacement. Two label errors of mine were also corrected, a blockquote wrongly excluded and a paragraph with an inline link wrongly omitted. MEASURED across 17 files: babel, markdown, html, json-locale, vue, svelte and css all at precision 100%, recall 100%, range accuracy 100%, with placeholder and unsafe-label accuracy 100% wherever labelled. Results: pnpm check 185 unit tests across 31 files; pnpm build passed; 40 integration tests passed.
- Next action / remaining acceptance: Still far short of the specification, which expects hundreds of fixtures: 17 files means the gate rejects regressions on these cases, not that extraction is correct in general. Highest-value additions next: real-world JSX patterns such as configured visible props and UI call functions, MDX expression boundaries, YAML locale edge cases, right-to-left and CJK content, and files large enough to exercise the size and timeout limits. Markdown table cells remain unextracted pending a decision on adding remark-gfm, which the specification implies since it lists table text as reviewable content.
