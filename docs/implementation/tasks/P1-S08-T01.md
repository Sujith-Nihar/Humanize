# P1-S08-T01 — Resolve trusted configuration

Status: in_progress

## Objective and prerequisites

Resolve trusted configuration. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S07. Requirements: FR-11, FR-12, FR-13, FR-14, FR-20, FR-21, NFR-05.
Upstream tasks: P1-S10-005, P1-S10-006.

## Implementation scope

Expected modules: `config`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Trusted-base precedence and administrator-only fields enforced.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T19:18:57.791108+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Trusted configuration resolution implemented and now WIRED TO A CALLER, which was the gap this task carried. packages/config parses .humanize.yml as hostile input (64 KiB cap, alias expansion capped, custom tags refused, mapping required), rejects any administrator-only key outright rather than ignoring it, lets a repository narrow review but never widen it, clamps the inline budget to the administrator cap, and fails closed to policy defaults so a malformed file cannot switch reviewing off. The event handler in apps/worker now reads that file through a TrustedFileSource port AT THE BASE REF, never the head, so a pull request cannot change the rules it will be judged by (ADR-027); it resolves the effective configuration against administrator policy, builds the ReviewSnapshot pinning configSha and the configuration digest, creates the review run and enqueues it. An integration test proves the read targets the base ref and the configuration path, that execution and retention in the stored snapshot come from policy rather than the repository file, that the run is persisted in state RECEIVED, and that a redelivery reuses the same run and produces the same idempotency key rather than a second review. Earlier finding retained: Zod 4 z.record() with an enum key is exhaustive, which silently invalidated any partial category map until it was changed to z.partialRecord. Results: pnpm check 191 unit tests across 31 files; pnpm build passed; 46 integration tests passed.
- Next action / remaining acceptance: Remaining: no production TrustedFileSource exists, so nothing yet fetches the configuration file from GitHub; that binding needs App credentials. Organization policy is supplied through an injected function and has no persistence or administration surface, which is P1-S16-T02. Explicit learnings remain part of the documented precedence chain with no store behind them (P1-S17-T01). Path-specific configuration is resolved by the package but the caller passes no path, so per-path overrides are not yet applied per node.
