# P1-S13-T05 — Apply PR budget and generate summaries

Status: in_progress

## Objective and prerequisites

Apply PR budget and generate summaries. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S13-T04. Requirements: FR-13, FR-14, FR-15, NFR-05.
Upstream tasks: P1-S8-009, P1-S8-010.

## Implementation scope

Expected modules: `review`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Five subjective active comments and nit defaults enforced.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T18:18:19.616791+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Noise policy implemented and enforced in packages/review/src/ranking.ts. planPublication holds subjective findings to five inline comments with the remainder moved to the summary, exempts deterministic policy violations from that budget so a configured rule is never crowded out, excludes nitpicks by default while keeping deterministic ones, and clamps a configured budget to the product default so configuration cannot raise it. Deduplication merges findings sharing a fingerprint, overlapping the same range in the same category, proposing the same replacement for the same place, or simply landing on the same node in the same category, which the gold set proved necessary when one node produced four comments. Ordering is deterministic, so the same review yields the same comments. 9 unit tests cover the budget, overflow to summary, blocking findings keeping their slot, nitpick defaults, and identical plans regardless of input order. Results: pnpm check 191 unit tests across 31 files.
- Next action / remaining acceptance: Scoring coefficients remain a documented starting point rather than an evaluation-derived model; the specification requires them to be tuned against the labelled corpus. Feedback-informed ranking is not implemented and belongs with P1-S17.
