# P1-S19-T02 — Run adversarial security review

Status: in_progress

## Objective and prerequisites

Run adversarial security review. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S19-T01. Requirements: SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, NFR-03, NFR-04, NFR-05, NFR-06.
Upstream tasks: P1-S11-009, P1-S11-010.

## Implementation scope

Expected modules: `testing`, `docs/security`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

No unresolved critical/high issues; injection/tenant/secret tests pass.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T21:57:39.573399+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Adversarial review executed against the real components and recorded in docs/security/adversarial-review.md. An attack corpus in packages/testing covers eight prompt-injection shapes, fabricated evidence, unsafe patches, six parser-hostility inputs and credential-shaped secrets; evals/security/boundaries.test.ts runs it against real extraction, prompt construction, the review pipeline, the suggestion validator and telemetry rather than doubles. HELD: injection is fenced with an unguessable per-review marker that content cannot close, and independently a model that obeys an injection still publishes nothing because the gate refuses any quotation absent from the node, so neither layer is load-bearing alone; fabricated quotations, paths and traversals are refused; unsafe patches are refused or neutralised by encoding with the structural proof rejecting anything beyond the target text; cross-tenant access is refused before the store is reached, on every endpoint and store that takes an organization. TWO FINDINGS, BOTH FIXED. First, twenty thousand nodes in one well-under-size file took 10.9 seconds to extract, because a size cap does not imply a node cap; extraction now stops at LIMITS.nodesPerFile and reports NODE_LIMIT_REACHED so a partially reviewed file cannot look fully reviewed, and the corpus now completes in 1.2 seconds. Second, telemetry redaction filtered by field NAME only, so a connection string logged as event or a token as traceId was written verbatim; values are now scrubbed by shape wherever they appear, with ordinary operational values untouched. Results: pnpm check 208 unit tests across 35 files; pnpm build passed; 70 integration tests passed.
- Next action / remaining acceptance: NOT SATISFIED: the task requires no unresolved critical or high issues after a review, and this is an agent review rather than an independent audit; a human security reviewer must repeat it before the criterion is met. Recorded residual limits: secret scrubbing works on shape and cannot catch an arbitrary short opaque secret, so callers must still not log secrets; no live GitHub or cloud provider was involved, so real token scope and real webhook delivery are untested; no dependency or image vulnerability scan, SBOM or signing exists (P1-S20); and there is no global rate limit on unauthenticated registration attempts or webhook volume.
