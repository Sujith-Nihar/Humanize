# P1-S17-T04 — Implement retention transitions and purge

Status: in_progress

## Objective and prerequisites

Implement retention transitions and purge. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S17-T03. Requirements: FR-22, FR-23, FR-24.
Upstream tasks: P1-S10-009.

## Implementation scope

Expected modules: `db`, `apps/api`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Old jobs cannot recreate purged data; backups disclose expiry.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T20:47:23.794230+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Retention transitions and purge implemented, with the acceptance property proven: a job queued before a purge cannot recreate the data the purge removed. RetentionStore.setMode changes the repository retention mode FIRST and within the same transaction as the purge, so any later write is refused by the column constraints that enforce ephemeral storage rather than relying on the caller to notice. Downgrading to ephemeral nulls finding explanations and replacements, nulls evidence quotes, deletes any held publication payload and marks the runs ephemeral; a test writes content-bearing rows while indexed, downgrades, and asserts no sentinel string survives anywhere. A separate test then attempts the write a stale job would perform and asserts the database refuses it and no content appears. purgeOlderThan removes runs past the retention window together with everything hanging off them, verified across review runs, findings and feedback. 3 PostgreSQL tests. Results: pnpm check 200 unit tests across 33 files; pnpm build passed; 70 integration tests passed.
- Next action / remaining acceptance: No schedule drives the purge: purgeOlderThan exists but nothing calls it periodically, and the retention window is a caller argument rather than configured policy. The specification also requires that backups disclose expiry, which is untouched: nothing records or communicates when purged data leaves backups, and that belongs with the backup runbook in P1-S20-T02.
