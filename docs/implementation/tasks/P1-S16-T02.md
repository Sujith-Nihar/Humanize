# P1-S16-T02 — Manage repositories and organization policy

Status: in_progress

## Objective and prerequisites

Manage repositories and organization policy. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S16-T01. Requirements: FR-01, FR-19, FR-21, FR-23, SEC-04, SEC-05.
Upstream tasks: P1-S10-002.

## Implementation scope

Expected modules: `apps/api`, `apps/dashboard`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Fresh all-enabled-repository admin checks serialized with enablement.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T20:44:03.247086+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Repository enablement and organization policy administration implemented, exposed and consumed. The store performs the administrator check INSIDE the write, holding the target rows FOR UPDATE and only then asking which the caller may administer, so the answer cannot go stale and a revocation that lands first wins; six PostgreSQL tests prove selective enablement, the check seeing exactly the locked rows, a revoked caller enabling nothing, concurrent requests never overlapping inside the check, and a foreign repository never being offered. Organization policy has its own versioned, tenant-isolated table (migration 0005) kept apart from repository_configs so repository content cannot reach administrator-only fields (ADR-027). HTTP endpoints expose listing, enablement, and reading or replacing policy behind session authorization, with a test proving every route refuses another organization with 403 while the store is never called, refuses an unsigned caller with 401, and rejects a malformed identifier with 400. The loop is now closed: the event handler reads the stored policy rather than taking an injected function, and A REVIEW IS SKIPPED RATHER THAN DEFAULTED when no policy is set, because running on a provider, retention mode or execution mode nobody chose would be worse than not running; a stored policy that no longer satisfies its schema is treated as absent rather than patched. An integration test proves the stored policy governs the resulting snapshot and that both absent and invalid policies skip. Results: pnpm check 200 unit tests across 33 files; pnpm build passed; 63 integration tests passed from a clean disposable database with migrations 0000-0005 applying from scratch.
- Next action / remaining acceptance: AdminCheck has no production implementation, so nothing resolves real GitHub repository administrators; that needs App credentials. Dashboard pages remain a scaffold, so administration is reachable by HTTP but has no interface, and the Playwright coverage S16-T04 expects does not exist.
