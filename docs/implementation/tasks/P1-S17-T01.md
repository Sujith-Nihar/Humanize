# P1-S17-T01 — Manage scoped explicit learnings

Status: in_progress

## Objective and prerequisites

Manage scoped explicit learnings. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S16. Requirements: FR-22, FR-23, FR-24.
Upstream tasks: P1-S10-007.

## Implementation scope

Expected modules: `apps/api`, `apps/dashboard`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

CRUD and precedence/auth tests pass.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T20:47:23.697878+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Scoped explicit learnings implemented in packages/db/src/knowledge.ts with migration 0006_medical_sally_floyd.sql. A learning carries path globs, a rule, an enabled flag and the administrator who wrote it, scoped by tenant and repository. forPath returns only enabled learnings whose globs match the file, which is what the configuration precedence chain needs. A disabled learning is kept rather than deleted so an administrator can still see what was once decided. Empty scopes and blank rules are refused. Tenant isolation is proven directly: listing through another organization returns nothing, and enabling or removing through another organization changes nothing and reports failure rather than succeeding silently. 3 PostgreSQL tests cover scoping, the enable and remove lifecycle, and cross-tenant refusal. Results: pnpm check 200 unit tests across 33 files; pnpm build passed; 70 integration tests passed from a clean disposable database with migrations 0000-0006 applying from scratch.
- Next action / remaining acceptance: Not yet consumed: the review pipeline does not read learnings, so the precedence chain still resolves without them and a scoped rule has no effect on a review. HTTP endpoints and dashboard management do not exist, so learnings can only be created through the store.
