# P1-S17-T02 — Record explicit and inferred feedback

Status: in_progress

## Objective and prerequisites

Record explicit and inferred feedback. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S17-T01. Requirements: FR-22, FR-23, FR-24.
Upstream tasks: Specification sections and approved execution-plan addition.

## Implementation scope

Expected modules: `apps/api`, `db`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Provenance visible; dismissals never create permanent rules.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T20:47:23.753299+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Feedback recording implemented with provenance preserved. Each record carries the finding fingerprint, one of the seven documented outcomes, whether it came from a person or was inferred, and the actor where there is one; the outcome and source sets are enforced by database constraints rather than convention, and repeated identical feedback is idempotent rather than multiplying. A test asserts that inferred feedback is marked as such with a null actor, so nobody mistakes a system observation for a person judgement. THE CENTRAL PROPERTY IS PROVEN: recording a dismissal creates no learning, so a single dismissal never becomes a permanent rule (spec 25.2); promoting feedback into a rule remains a deliberate administrator action through LearningStore. Results: pnpm check 200 unit tests across 33 files; 70 integration tests passed.
- Next action / remaining acceptance: No caller records feedback yet: nothing observes an accepted suggestion, a dismissed comment or a finding resolved by a later commit, so the table is correct but empty in practice. Feedback does not influence ranking, which the specification permits to remain future work but which is the point of collecting it.
