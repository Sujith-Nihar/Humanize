# P1-S14-T03 — Apply check and incomplete-review policy

Status: in_progress

## Objective and prerequisites

Apply check and incomplete-review policy. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S14-T02. Requirements: FR-17, SEC-07, NFR-02.
Upstream tasks: P1-S9-008.

## Implementation scope

Expected modules: `github`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Subjective findings neutral; configured deterministic violations fail.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T19:41:20.289201+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Publish worker implemented, and the ADR-038 lifetime guarantee is now proven at worker level rather than only at the store. publishReview takes the held payload, publishes it, and discards the content on every path that will not publish later: after a successful publish, after a superseded head where a newer review supersedes this one, and after a payload that cannot be parsed and so could never publish. It keeps the payload only for a failure a retry could still resolve, because discarding then would lose a review the queue is about to attempt again; the retry then publishes and clears it. 5 PostgreSQL integration tests assert the payload row count directly after each path, including that the transport is never called for an unpublishable payload. apps/worker/src/main.ts is now a runnable process: it migrates, starts the queue, SWEEPS ANY PAYLOAD A PREVIOUS PROCESS LEFT BEHIND before taking new work so content never outlives a crash by more than one restart, subscribes to github.event and review.publish, rehydrates each event from its recorded delivery rather than from the job payload, which carries identifiers only and never content, signals a retry by throwing so pg-boss reschedules, and shuts down on SIGTERM and SIGINT. Results: pnpm check 191 unit tests across 31 files; pnpm build passed; 56 integration tests passed from a freshly created disposable database.
- Next action / remaining acceptance: The worker starts but cannot yet complete a review: the publish transport throws PUBLISH_TRANSPORT_UNBOUND and the github.event handler is wired without a TrustedFileSource, so no review run is created. Both bindings need GitHub App credentials. The retention sweep runs once at startup rather than on a schedule, so a long-running process does not yet reclaim payloads abandoned during its own lifetime.
