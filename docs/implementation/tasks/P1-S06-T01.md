# P1-S06-T01 — Implement App token broker

Status: in_progress

## Objective and prerequisites

Implement App token broker. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S05. Requirements: FR-01, FR-02, FR-17, SEC-04, SEC-06, NFR-02.
Upstream tasks: P1-S2-001.

## Implementation scope

Expected modules: `github`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Live test App proves repository and permission scope.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-20T21:31:46.144836+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: App token broker verified against LIVE GitHub for the first time. GitHub App humanize-superhuman (id 5013754) authenticates with its private key; GET /app reports permissions checks=write, contents=read, metadata=read, pull_requests=write, so Contents stays read-only and INV-011 holds in the real App. GitHubTokenBroker.scopedToken minted a real installation access token for installation 163328826, and GET /installation/repositories confirms it is scoped to exactly ONE repository. Read and publish roles return distinct tokens, and the cache returns the same read token within its window. The previous record that no real installation access token had ever been minted is now superseded.
- Next action / remaining acceptance: Not yet proven live: that a read-scoped token is actually refused write access. The negative test issues a write request to GitHub and was deliberately not run without explicit approval. Token expiry, revocation on installation suspension, and cache eviction across the one-hour lifetime also remain unverified against the live App.
