# P1-S04-T07 — Implement connection probes and contract suite

Status: in_progress

## Objective and prerequisites

Implement connection probes and contract suite. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S04-T06. Requirements: FR-18, SEC-03, SEC-04.
Upstream tasks: P1-S6-007, P1-S11-005.

## Implementation scope

Expected modules: `providers`, `evals/provider-contracts`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

All four providers pass application-shaped probes.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T16:12:54.719241+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: FIRST LIVE PROVIDER VERIFICATION IN THIS PROJECT. A deliberate live lane was added (vitest.live.config.ts, pnpm test:live, matching *.live.test.ts, excluded from the default and integration lanes so pnpm check never requires a running service). Against a real local Ollama (qwen3.5:4b, 4.7B Q4_K_M, declared context 262144) all 5 contract tests in packages/providers/ollama.live.test.ts passed: the application-shaped capability probe, the real ReviewerResponseSchema returning a schema-valid response for content deserving a finding, the same schema returning an empty candidate list for acceptable content, VerificationSchema returning a verdict bound to the exact ADR-036 candidate identity supplied, and rejection of a model that is not installed. TWO LIVE-ONLY DEFECTS FOUND, both invisible to mocked tests. First, the adapter never set num_ctx; Ollama defaults to a small window and silently discards the overflow, and chat truncation drops the oldest message first, which is the system prompt carrying the untrusted-content boundary, so quality and the prompt-injection defence would degrade together. Second, a model advertising the thinking capability spends the bounded output budget on reasoning and never emits JSON: every pipeline call failed with CONTEXT_LIMIT after about 55 seconds. The adapter now requests num_ctx covering the full budget and sends think:false, but only to a model whose /api/show capabilities declare thinking, since the field is invalid otherwise; the capability is resolved once per model and cached. Measured effect: an identical structured request went from exhausting 4000 output tokens and failing to completing in 57 tokens and 1.1 seconds. Results: pnpm check 114 unit tests across 20 files; pnpm build passed; 40 integration tests passed; 8 live tests passed in 25.8 seconds.
- Next action / remaining acceptance: Live evidence covers Ollama only. OpenAI, Gemini and OpenRouter remain entirely unverified against live services and no credentials are present, so the acceptance that all four providers pass application-shaped probes is NOT met. Live coverage is also single-model: qwen3.5:4b passing establishes nothing about other local models, and a model must be confirmed to support at least a 16000-token window.
