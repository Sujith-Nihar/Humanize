# P1-S14-T01 — Fence publishing and recheck freshness

Status: in_progress

## Objective and prerequisites

Fence publishing and recheck freshness. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S13. Requirements: FR-17, SEC-07, NFR-02.
Upstream tasks: P1-S9-006.

## Implementation scope

Expected modules: `github`, `apps/api`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

Head races cannot remain classified as current completed feedback.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-20T23:39:39.998159+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Publication is wired and PROVEN AGAINST LIVE GITHUB. The full path runs end to end: webhook, durable delivery, event reconciliation, trusted configuration read from the base ref, review run created, runner lease, private clone with a job-scoped token, extraction, review, result upload, independent validation, ranking, and publication. Measured on a real private pull request: check run 106169858024 completed with conclusion success and a summary comment posted by humanize-superhuman[bot] reading "Reviewed 12 changed pieces of user-visible content. Nothing to flag." Comment coordinates come from GitHub through fetchDiffMap over the pull request files API, never from the runner, so a compromised runner cannot move a finding onto a line the author never wrote (INV-014). The publish credential is repository-scoped and carries pull_requests and checks write only. TWO UNBOUND SEAMS WERE FOUND BY RUNNING IT: pull_request.review had no subscriber and RunnerStore.enqueue was never called in production code, so no lease was ever offered to a runner; and the API bound no PublicationScheduler, so an accepted result reached the end of the pipeline and stopped. pnpm check passed with 224 unit tests across 34 files.
- Next action / remaining acceptance: Not yet exercised live: the stale-head race, since no publication has raced a new commit; reconciliation of an uncertain write after a timeout; and an inline comment, because this run produced no findings to place. Published comment identifiers are still not persisted, so re-review reconciliation relies on the marker alone.
