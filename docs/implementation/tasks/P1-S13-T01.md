# P1-S13-T01 — Complete category schemas and evidence validation

Status: in_progress

## Objective and prerequisites

Complete category schemas and evidence validation. Follow the approved [architecture](../../architecture/README.md) and [agent policy](../../../AGENTS.md).

Prerequisite: P1-S12. Requirements: FR-13, FR-14, FR-15, NFR-05.
Upstream tasks: P1-S8-003, P1-S8-004.

## Implementation scope

Expected modules: `review`.

1. Inspect existing implementation and verify prerequisite evidence.
2. Read the relevant source specification and architecture contracts.
3. Verify dependency/API versions and state the concrete implementation steps.
4. Implement this task within its module boundary and runtime schemas.
5. Add success and failure tests; include tenant authorization, retry, cancellation and concurrency tests wherever this task crosses those boundaries.
6. Run acceptance checks and record reproducible evidence below.

Database changes: only versioned migrations if this task owns persistence; no ad hoc schema changes. See [data lifecycle](../../architecture/data.md).
Interfaces: preserve the versioned contracts in [architecture](../../architecture/README.md); no provider-specific logic in review or unscoped persistence.

## Acceptance and tests

All categories include valid/invalid evidence and authorship rejection.

Run `pnpm check` plus relevant database, provider, GitHub, runner or evaluation checks. Documentation-only tasks use `python3 scripts/check_docs.py`. Never count absent credentials, services or human labels as passing evidence.

## Non-goals

No work belonging to later tasks, no customer-code execution, no weakened privacy or authorization, no unapproved infrastructure. The complete product scope remains in the preserved specification.

## Completion evidence

- Last updated: 2026-09-18T16:30:58.565459+00:00.
- Implementation revision: current working tree; no commit claimed.
- Evidence: Ranking, deduplication and the noise budget implemented in packages/review/src/ranking.ts, taken ahead of its stage because live testing showed the model returning a finding for a whole sentence and another for a sub-phrase of it, which would have spammed the first pull request Humanize commented on. scoreFinding combines the lower of reviewer and verifier confidence, a severity weight, evidence quality, the node visibility confidence and actionability, and is deterministic. planPublication merges findings that share a fingerprint, that overlap the same source range in the same category, or that propose the same replacement for the same place, keeping the higher-scoring statement; it then holds subjective findings to five inline comments with the remainder moved to the summary, exempts deterministic policy violations from the budget, and excludes nitpicks by default while keeping deterministic ones. The configured budget is clamped to the product default so configuration cannot raise it. 8 unit tests cover scoring order and stability, the sentence-versus-sub-phrase merge that live testing exposed, distinct places and categories kept apart, same-replacement merging, budget overflow to summary, blocking findings never losing their slot, nitpick exclusion, and identical plans regardless of input order. Results: pnpm check 127 unit tests across 22 files.
- Next action / remaining acceptance: Coefficients are a documented starting point, not a calibrated model; the specification requires them to be evaluation-driven and they must be tuned against the labelled corpus in P1-S08-T05 before release. Remaining in this stage: the review summary generator, feedback-informed ranking, and confirming the budget end to end once the publisher exists. No caller applies planPublication yet; the publisher is next.
